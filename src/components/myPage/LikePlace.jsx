import React from 'react';
import styled from 'styled-components';

const LikePlaceBlock = styled.div``;

const LikePlace = () => {
  return <LikePlaceBlock>AuthForm</LikePlaceBlock>;
};

export default LikePlace;
